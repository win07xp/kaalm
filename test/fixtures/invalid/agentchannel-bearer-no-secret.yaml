# conditional-required: bearer auth needs secretRef
apiVersion: kaalm.io/v1beta1
kind: AgentChannel
metadata: {name: bearer-nosecret, namespace: default}
spec:
  agentRef: {name: support-assistant}
  webhook:
    path: /channels/default/bearer-nosecret
    auth: {type: bearer}
