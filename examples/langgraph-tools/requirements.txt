# Pinned against the LangGraph 1.x line current at authoring time. langchain
# provides create_agent; langchain-mcp-adapters pins mcp to 1.x, which rides
# httpx 0.x like the base image.
langgraph==1.2.11
langchain==1.3.15
langchain-openai==1.4.3
langchain-mcp-adapters==0.3.2
