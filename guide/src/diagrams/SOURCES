# Figures the guide embeds, one file name per line. Each is rendered from
# docs/src/diagrams/<name>.puml and copied here by `make diagrams`, so the
# design book stays the single source. `make docs-check` fails if a copy
# drifts from its source.
agent-lifecycle.svg
