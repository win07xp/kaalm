# Installation

*Stub: to be written.*

This page will cover:

- Prerequisites: cert-manager (with `--enable-certificate-owner-ref=true`),
  trust-manager, and a NetworkPolicy-enforcing CNI.
- Installing the Helm chart and choosing an adoption tier.
- Verifying the install: the five CRDs, the controller, and the gateway.

Until a chart is published (a v0.2.0 milestone), installation means building
from source; see the repository README.
