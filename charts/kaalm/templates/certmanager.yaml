{{/*
The Kaalm trust chain. cert-manager and trust-manager controllers are hard
prerequisites the chart does NOT install; it ships only the Kaalm-specific
issuers, certificates, and bundle. See docs/src/security/tls.md.
*/}}
---
# A self-signed ClusterIssuer that bootstraps the Kaalm root CA.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: kaalm-selfsigned
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
# The Kaalm root CA. It and its output Secret MUST live in cert-manager's
# cluster resource namespace, or the CA ClusterIssuer below fails cluster-wide
# with SecretNotFound (the ca.secretName has no namespace field).
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kaalm-ca
  namespace: {{ .Values.certManager.clusterResourceNamespace }}
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  isCA: true
  commonName: kaalm-ca
  secretName: kaalm-ca
  duration: 43800h    # 5y
  renewBefore: 8760h  # 1y
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Never  # key reuse on renewal keeps all leaves valid
  issuerRef:
    name: kaalm-selfsigned
    kind: ClusterIssuer
---
# The CA ClusterIssuer that signs every Kaalm leaf, including the per-Agent
# and per-AgentTask certs created in user namespaces.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: kaalm-ca-issuer
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  ca:
    secretName: kaalm-ca
---
# The gateway serving/client certificate.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kaalm-gateway-tls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  secretName: kaalm-gateway-tls
  duration: 2160h     # 90d
  renewBefore: 720h   # 30d
  issuerRef:
    name: kaalm-ca-issuer
    kind: ClusterIssuer
  usages:
    - server auth
    - client auth
  dnsNames:
    - kaalm-gateway.{{ .Release.Namespace }}.svc.cluster.local
    - kaalm-gateway.{{ .Release.Namespace }}.svc
    - localhost
    {{- range .Values.gateway.externalHostnames }}
    - {{ . | quote }}
    {{- end }}
---
# The controller serving/client certificate for the activator, activity API,
# and channels-health endpoints on :9443.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kaalm-controller-tls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  secretName: kaalm-controller-tls
  duration: 2160h
  renewBefore: 720h
  issuerRef:
    name: kaalm-ca-issuer
    kind: ClusterIssuer
  usages:
    - server auth
    - client auth
  dnsNames:
    - kaalm-controller.{{ .Release.Namespace }}.svc.cluster.local
    - kaalm-controller.{{ .Release.Namespace }}.svc
---
# trust-manager projects the CA into workload namespaces as the kaalm-ca
# ConfigMap (mounted at /var/run/kaalm/ca.crt). trust-manager reads Bundle
# sources only from its trust namespace, which must equal the cluster resource
# namespace above.
apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: kaalm-ca
  labels:
    {{- include "kaalm.labels" . | nindent 4 }}
spec:
  sources:
    - secret:
        name: kaalm-ca
        key: tls.crt
  target:
    configMap:
      key: ca.crt
    {{- if .Values.trustManager.bundleSelector }}
    namespaceSelector:
      {{- toYaml .Values.trustManager.bundleSelector | nindent 6 }}
    {{- end }}
