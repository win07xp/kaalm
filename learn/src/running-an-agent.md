# Running an Agent

*Stub: written after v0.2.0.*

- The three YAML files, applied and explained one at a time: the provider
  (your API key, kept safe), the class (the rules), the agent itself.
- Vocabulary on first contact: Secret, PVC.
- Watching `kubectl get agents -w` walk from Provisioning to Running, with
  the real expected output shown.
- One paragraph on what happened behind the scenes, then a link to the
  design book for readers who want the whole story.
