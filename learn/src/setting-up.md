# Setting Up Your Laptop

*Stub: written after v0.2.0.*

- The four tools and why each exists: docker (runs containers), k3d (a
  Kubernetes cluster inside docker), kubectl (the remote control), helm
  (the installer).
- Install commands per platform, each with a version check to confirm it
  worked.
- First vocabulary paragraph: what a cluster is.
- An LLM API key, and which providers work.
